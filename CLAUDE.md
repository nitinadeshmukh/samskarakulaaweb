# CLAUDE.md — samskarakulaaweb

Marketing site for **Samskara Kula**, deployed at `https://samskarakula.com`. Built 2026-07-06, split-adjacent to the `samskarakulaaapi` (backend) and `samskarakulaaapp` (mobile apps) repos.

## Domain terms (use verbatim, see full glossary in the other two repos)

Samskara, Shodasha Samskaras, Gotra, Veda/Shakha, Kula Devata, Muhurta, Panchang/Ephemeris, Pandit/Purohit, Agamic tradition, Samagri, Havan/Homam. Do not paraphrase these into generic marketing-speak in copy — they're precise, and getting them wrong reads as inauthentic to the target audience.

## Tech stack

- Vite + React + TypeScript + Tailwind CSS, static build output (`dist/`).
- No backend of its own — this is a static marketing site. It intentionally does NOT talk to the Supabase backend (`samskarakulaaapi`) or reuse any app auth/data.
- Branding: genealogy-constellation mark (`src/components/Logo.tsx`), blue/navy (`kula-navy` `#0E4F9E`) + gold/amber (`kula-gold`/`kula-amber`) palette — matches `samskarakulaaapp`'s customer-app icon and UI accent color.

## Design system (commercial redesign, 2026-07-08)

Per direct user request ("The look and feel of the Website and app should be commercial, get a decent UI from Mobin") — Mobbin.com (the likely-intended reference, mobin.com resolves to an unrelated travel site) returned `403` to automated fetches, so the redesign draws on established commercial-app conventions rather than a live browse of that site; flag this if a future ask assumes Mobbin was actually consulted.

- **Typography**: `Inter` (body/UI) + `Fraunces` (headings/display) loaded via Google Fonts `<link>` in `index.html` — `font-sans`/`font-display` in `tailwind.config.js`. Kept the existing `kula-navy`/`kula-gold`/`kula-amber` accent palette per explicit user instruction ("keep saffron/orange as the accent"); added `kula-navyDeep` (`#071F3F`) for a richer hero/dark-section background and `kula-cream`.
- **Reveal component** (`src/components/Reveal.tsx`) — lightweight IntersectionObserver fade-up-on-scroll wrapper, no motion library installed/needed.
- **`.eyebrow`/`.bg-aurora`** utility classes in `src/index.css` — small uppercase label above section headings, and a CSS-only radial-gradient "glow" background used behind the hero/CTA/how-it-works sections instead of image assets.
- **Nav** (`src/sections/Nav.tsx`) — added a working mobile hamburger menu (previously: nav links were completely absent below the `sm` breakpoint, a real functional gap, not just a visual one). Header background is solid `kula-navyDeep` always, not translucent — a translucent header blended with the page's white background (not the navy hero) since the header sits *before* the hero in the DOM rather than overlapping it, producing a washed-out slate color; this was caught and fixed via live browser-preview screenshot during this pass.
- **Hero/Priests sections** now include composed CSS/SVG mockups (`CalendarMock` in `Hero.tsx`, `PriestAppMock` in `Priests.tsx`) in place of a flat placeholder box ("Priest companion app screenshot" text) — original compositions, not copies of any real screenshot (none exists yet).
- **No fabricated social proof** — the hero's "trust strip" (16 Samskaras covered / background-checked priests / tamper-evident proof) uses factual product claims already elsewhere in this doc, not invented testimonials or user counts.

## Justinmind-style pass (2026-07-08, same day)

Per direct user request ("Can you do the UI like justinmind.com site") — unlike Mobbin, justinmind.com loaded fine in a real browser (`claude-in-chrome`, not `WebFetch` — their site doesn't block a real browser session), so this was built from an actual live look at their homepage, not a from-memory guess. Scoped to **website only** (not the apps) and **kept the saffron/amber palette** rather than switching to Justinmind's actual purple/magenta — both confirmed explicitly with the user before starting, since a full purple switch would have contradicted the "keep saffron/orange" instruction from the redesign earlier the same day. This pass borrows Justinmind's *structural* patterns, not their color.

- **`src/components/Squiggles.tsx`** — hand-drawn-style white line-doodle SVGs, scattered in the hero's corners. **`src/components/WaveDivider.tsx`** — reusable wavy-bottom-edge SVG divider (the "colored section flows into a curved shape" transition Justinmind uses between their purple hero and the white content below). **`src/components/GeoAccents.tsx`** — `GeoCircle`/`GeoTriangle`/`GeoDots`, playful geometric accent shapes scattered near illustrations (their site uses a pastel circle, a solid triangle, and a dotted-grid square next to feature illustrations) — original shapes in our palette (gold/amber/navy), not a copy of their specific artwork.
- **`Nav.tsx`** — changed from the always-navy header (from the commercial-redesign pass above) to a plain **white bar** with a **black pill CTA button** — this matches what Justinmind's nav actually does (solid white, sitting above their colored hero) more closely than a colored nav would.
- **`Hero.tsx`** — full rebuild: background changed from the navy `bg-aurora` treatment to a vibrant **`from-orange-600 via-kula-amber to-kula-gold`** diagonal gradient (our own hue family, standing in for their purple); layout changed from two-column (text + mockup side by side) to **single-column centered**, matching their homepage; headline switched from the serif `font-display` to **heavy black-weight sans** (`font-sans font-black`) at a much larger size — Justinmind's headline is bold sans, not serif, and this is the single most visually defining trait of their homepage; primary CTA is now a **black pill button** (was amber) — the signature high-contrast-black-button-on-colored-background pattern; the `CalendarMock` card was restyled from a dark-glass card (worked on navy) to a **white card with a thick black border** (a "sketch" look) since it now sits on a bright orange background instead of navy, and moved below the CTA/trust-strip, straddling the new `WaveDivider` transition into the Problem section — the floating-screenshot-over-the-wave effect Justinmind uses.
- **`Problem.tsx`** / **`Priests.tsx`** — added `GeoDots`/`GeoCircle`/`GeoTriangle` accents near the section heading and the phone mockup respectively, at a light touch (not scattered on every section, to avoid visual overload) — `HowItWorks.tsx`/`Waitlist.tsx`/`Footer.tsx` were left as-is from the earlier commercial-redesign pass since they already had dedicated treatments (numbered-step connector line, navy CTA card) that didn't need Justinmind-style accents layered on top.
- **Deliberately did not copy** Justinmind's actual "Trusted by Adobe / Google / Kroger / IRS / Verizon / Cisco / Siemens / Sony" grayscale logo strip — Samskara Kula has no such partnerships, and displaying real company logos without a relationship would be a fabricated-trust-signal problem, not just an aesthetic one. No equivalent section was added.
- Verified live in browser preview at 1440px: hero gradient/squiggles/wave/floating-mockup, white nav bar, and the Priests section's geometric accents (circle/triangle/dots correctly positioned around the phone mockup) — one screenshot capture briefly rendered a visually-compressed/narrow-looking frame despite the DOM actually being full-width (confirmed via `getBoundingClientRect`), same stale-capture-pipeline quirk as earlier in the day; restarting the preview server's capture resolved it, page code was never at fault.

## Services/symbols/ratings pass (2026-07-08, same day)

Per direct user request ("This site on landing screen should have a showcase of services, ratings, reviews and offcourse the message that everyone should never miss a ritual... Use the 24 powerful symbols of hindus where ever possible"). Two things were confirmed with the user before building, both flagged proactively rather than guessed at:

- **"24 powerful symbols"** — there is no single universally standard canonical list of 24 Hindu symbols. Asked the user, who confirmed: a general set of commonly-recognized sacred symbols/icons (not a specific documented "24," e.g. not the 24 forms of Vishnu/Chaturvimshati Murti). `src/data/hinduSymbols.ts` holds the compiled 24-item list (name + one-line meaning). **The swastika is listed there for completeness/accuracy but deliberately not rendered as a standalone visual icon anywhere on the site** — it's a genuine ancient symbol of auspiciousness unrelated to its unrelated 20th-century misappropriation, but a public-facing marketing site aimed partly at a Western-diaspora audience risks real misreading/backlash from displaying it visually, independent of its authentic meaning. This was a unilateral judgment call, not confirmed with the user — flag it if it should be reconsidered.
- **Ratings/reviews** — the product is pre-launch with zero real reviews (the `priest_reviews` table is real and live in the apps, but has no rows from actual families). Asked the user how to handle this given the site's own "no fabricated social proof" rule from the earlier redesign; they chose **illustrative sample reviews, clearly marked** over the more conservative "feature-only, no reviews" option. `RatingsReviews.tsx` implements this: an explicit disclaimer sentence ("Samskara Kula is pre-launch — the reviews below are illustrative examples... not real testimonials") directly under the heading, every card carries a black "SAMPLE" badge, and attribution is location-only ("A family in Fremont, CA") rather than invented personal names standing in for real people.
- **`src/components/SacredSymbolIcon.tsx`** — minimal 1.5–1.6px-stroke line-art icons for 10 of the 24 (Om — rendered as the actual ॐ glyph rather than a drawn approximation, Trishula, Shankha, Padma, Diya, Kalash, Damaru, Surya, Mala, Ghanti); the other 14 appear as name-only chips in `SacredSymbols.tsx` rather than force-fitting a bespoke icon to every entry.
- **`SacredSymbols.tsx`** (new, right after `Hero`) — carries the reinforced "never miss a ritual" marketing message as its heading ("Every samskara. Every generation. Never missed — wherever your family lives.") alongside the icon grid, folding the two asks together rather than adding a separate banner section.
- **`ServicesShowcase.tsx`** (new, between `Problem` and `Features`, anchor `#services`, added to `Nav.tsx`'s link list) — a grid of real bookable service names (Namakarana, Upanayana, Vivaha, Griha Pravesh, Satyanarayan Puja, Ganesh Chaturthi Puja, Ayushya Homam, Navagraha Shanti), not placeholders — the puja/homam names were taken from the actual live-seeded `ritual_services` catalog observed directly in `samskarakulaaapp`'s customer-app preview earlier in this session, not invented.
- **`RatingsReviews.tsx`** (new, between `HowItWorks` and `Priests`) — see the illustrative-reviews note above.
- Verified live in browser preview at 1280px: all three new sections render in the correct order (`Hero → SacredSymbols → Problem → ServicesShowcase → Features → HowItWorks → RatingsReviews → Priests → Waitlist → Footer`, confirmed via `document.querySelectorAll('h2')` after a screenshot-capture glitch briefly made this ambiguous — same stale-capture-pipeline issue as the Justinmind pass, resolved the same way), the 10 custom sacred-symbol icons render correctly, the services grid shows real names with category-appropriate icons, and the ratings section's SAMPLE badges/disclaimer are clearly visible.
- Verified live via browser preview screenshots at both mobile (375px) and desktop (1440px) widths; the only live-testing hiccup was a stuck screenshot-capture pipeline after several rapid interactions (confirmed via `document.body.innerText`/computed-style inspection that the page itself was rendering correctly throughout — restarting the preview server's capture pipeline resolved it, page code was never at fault).

## Logo replacement (2026-07-09)

Per direct user request (image attachment + "Use this as logo for everything"), scope confirmed with the user as covering all 4 products including native app icons/splash, not just the website. Full detail (checkerboard/transparency fix, per-app icon/monochrome regeneration) lives in `samskarakulaaapp`'s `CLAUDE.md` — this repo's slice of it:

- `Logo.tsx` was the old hand-drawn genealogy-constellation SVG (`kula-gold` gradient, inline `<svg>`); replaced with `<img src="/logo.png" .../>`, same `size`/`className` props, so `Nav.tsx`/`Hero.tsx`/`Footer.tsx` needed no changes. **The old SVG mark and this raster medallion are visually unrelated** — this is a full brand-mark replacement, not a refresh of the existing one, per the user's explicit "use this for everything."
- New `public/logo.png` (256x256) and `public/favicon.png` (64x64), both downscaled from the shared master (`samskarakulaaapp/design/om-logo/om-logo-master.png`) rather than duplicating the fix logic here. `index.html`'s favicon `<link>` now points at the PNG; `public/favicon.svg` is left in place but unused (not deleted, just superseded).
- Verified live in browser preview: `logo.png` loads (`200`) and renders correctly in the nav bar at 32px.
- Typecheck has one pre-existing, unrelated failure (`tsconfig.node.json` "may not disable emit") confirmed via `git stash` to predate this change — not introduced by the logo swap.

## Analytics (2026-07-08, same day) — the one deliberate exception to "no backend"

Per direct user request ("We will have to study user behaviours when on our site to provide them services, work on it so we can cater better"). Confirmed with the user first: track website **and** customer-app (not priest-app), self-hosted custom pipeline in Supabase (not a third-party vendor — data-residency/DPDP reasons per root `CLAUDE.md` §7 rule 6), consumed via an admin-app dashboard (not yet built — see `samskarakulaaapp`'s task list).

This is a **narrowly scoped exception** to "no backend of its own" above — the site now talks to Supabase for exactly one thing: write-only inserts into `analytics_events` (`samskarakulaaapi` migration 0017). Nothing else about the site's architecture changed; it does not read from Supabase, does not use auth, does not touch any other table.

- **`src/lib/analytics.ts`** — `trackEvent(eventName, properties)`, writes `{source: 'website', session_id, event_name, event_properties}`. `session_id` is a `crypto.randomUUID()` generated once and persisted in `localStorage` (`sk_session_id`) — anonymous, first-party, not a fingerprint, not shared cross-site. Only ever captures UI-navigation/interest signals (page view, section scrolled into view, button clicked) — **never** anything a visitor typed into a form (the waitlist email itself is not sent as an event property).
- **`src/hooks/useSectionView.ts`** — IntersectionObserver-based, fires a `section_viewed` event once per section per page load (threshold 0.3, disconnects after first fire). Deliberately separate from `Reveal.tsx` (that component fires per-item for the fade-up animation, too granular/noisy for section-level analytics).
- Wired: `page_view` (`App.tsx`, on mount), `section_viewed` × 6 sections (Problem, Features, HowItWorks, RatingsReviews, Priests, ServicesShowcase), `cta_clicked` on both Nav CTAs (desktop + mobile) and both Hero CTAs, `waitlist_submitted` on the waitlist form's actual submit handler.
- **Env**: `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` in `.env` (gitignored — this repo never had a `.env`/`@supabase/supabase-js` dependency before this pass, both newly added). Same anon key value already used publicly by the other 3 apps — reusing it is not a secret-handling issue, since the anon key is client-public by design and protected by RLS, not secrecy.
- **`src/vite-env.d.ts`** (new) — `/// <reference types="vite/client" />`. Needed once `import.meta.env` was actually used for the first time in this repo; without it, `tsc -b` fails with `Property 'env' does not exist on type 'ImportMeta'`. (Separately, this repo's `npm run typecheck` script itself has a pre-existing, unrelated bug — `tsc -b --noEmit` errors with `TS6310: ... may not disable emit` because `-b`/build-mode and a CLI `--noEmit` override conflict; this predates this session, reproduces on a clean `git stash`, and isn't part of the analytics work. Use plain `npx tsc -b` to actually typecheck this repo until the script is fixed.)

**A real bug was found and fixed during live verification, not just typechecked**: the original `trackEvent` implementation used `void supabase.from('analytics_events').insert({...})` as a "fire and forget" pattern. This looks reasonable for a normal `Promise` but is wrong for `@supabase/postgrest-js`'s query builder — it's a **lazy thenable**: the actual `fetch()` only happens when the builder is awaited or `.then()`'d. `void builder` alone evaluates the expression and discards it *without ever calling `.then()`*, so the request silently never fired. No analytics event had ever actually reached the database despite the code typechecking and looking correct. Confirmed via live network inspection (zero POST requests to `analytics_events` despite firing `trackEvent` multiple ways) before diagnosing the cause. Fixed by chaining `.then(() => {}, () => {})` to actually trigger execution while still swallowing both success and error paths silently (per the "should never break the page" comment already there). **Re-check this pattern if `trackEvent` or any other bare `supabase.from(...).insert(...)` call is ever written without an `await`/`.then()`/`.select()` — the lazy-thenable footgun applies to any postgrest-js builder, not just this one.**
- Live-verified end-to-end after the fix: reloading the page produces a real `page_view` row, clicking a "Join the Waitlist" CTA produces a real `cta_clicked` row with `{cta: "join-waitlist", location: "nav"}`, both confirmed via direct `psql` query against the production table (test rows cleaned up by exact ID afterward, not by pattern-match delete).
- The `analytics_events` RLS insert policy (`family_account_id is null or family_account_id in (select ... from family_accounts ...)`) is correct and was independently confirmed to work as designed once a `RETURNING`/`Prefer: return=representation` isn't attached to the request — a stray manual test that added that header hit the (also-correct, admin-only) `analytics_events_admin_read` SELECT policy instead, which was the source of an initial false alarm during debugging, not a real RLS bug. `supabase-js`'s `.insert()` does not request a representation unless `.select()` is chained, so this doesn't affect real usage.

## Known gaps (do not silently "fix" by fabricating a working backend)

- **Waitlist form** (`src/sections/Waitlist.tsx`) has no real collection endpoint — it's a local-only "thanks" state. Needs a real destination (Supabase table + Edge Function, or a third-party service) before it actually captures leads. Flag this explicitly if asked to "make the waitlist work."
- **"Apply to join the roster"** (priest recruitment CTA) currently links to the same waitlist anchor — there's no distinct priest-application flow yet.
- No app store badges/links exist yet (no published app store listing at time of writing) — don't add App Store/Play Store buttons that link nowhere.

## Conventions

- Keep this a static site (no SSR/API routes) unless a real requirement forces otherwise — simplest to deploy via nginx serving `dist/`, no Node process to keep alive on the VPS.
- Commits: conventional commits (`feat:`, `fix:`, `chore:`), scoped to the section/component touched.

## Deployment

**Confirmed live 2026-07-07**: nginx + TLS (certbot, expires 2026-10-05) configured on VPS `srv1776252.hstgr.cloud` (89.116.122.213, Mumbai — a *different* server than the backend, see `samskarakulaaapi`'s CLAUDE.md for the full VPS map). Static root is `/var/www/samskarakula.com`. Both `https://samskarakula.com` and `https://www.samskarakula.com` verified serving the real built site (not a placeholder) via a manual `scp` deploy — real HTML/JS/CSS confirmed live via curl.

**Manual deploy used an SSH key added specifically for this** (`samskarakula-website-deploy`, ed25519, public key appended to `/root/.ssh/authorized_keys` on that VPS with explicit user sign-off — this was NOT auto-approved, since it's standing access on a shared server). The private key lives outside any repo at `/Volumes/E/Claude/.deploy_keys/website_deploy_key` (gitignored location, never commit it). This same key is the natural candidate for the `VPS_SSH_KEY` GitHub Actions secret once the repo exists — reuse it rather than generating a second one, to avoid key sprawl on a shared VPS.

See `README.md` for the CI/CD summary once the GitHub repo is created.
